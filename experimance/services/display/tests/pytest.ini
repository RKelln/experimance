# This pytest config ensures that tests skip properly marked window-creating tests
# unless explicitly requested with the --display flag

[pytest]
markers = 
    display: marks tests that create real windows (skipped by default)

# Add the --display flag to enable tests that create real display windows
addopts = -v

# Async tests configuration
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
